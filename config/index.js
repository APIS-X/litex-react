// 系统名称
export const title = '中台系统';

// layout 相关配置
export const layoutSettings = {
  title,
  layout: 'mix',
  fixedHeader: true,
  fixSiderbar: true,
  splitMenus: true,
};
// theme设置
export const themes = {
  // colorPrimary: ''
};

export default {
  title,
  layoutSettings,
  themes,
};
